# chmura2020

## Running api:

`$ docker-compose up`

`$ ./scripts/migrate`

then visit:

`http://localhost:8080/api/products`

## Important files:
 - docker-compose.yml
 - service/Dockerfile
 - db_setup.env
